<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>Add more fruits and vegetables!</h1>
  <%= simple_form_for @fruit do |f| %>
    <%= f.input :name %>
    <%= f.input :fruitorveggie %>
    <%= f.input :benefits %>
    <br />
    <%= f.submit 'Add', class: 'btn btn-primary' %>
  <% end %>
</div>
